[package]
name = "rust-volunteer-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_volunteer_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }

